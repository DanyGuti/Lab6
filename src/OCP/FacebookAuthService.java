package OCP;
public class FacebookAuthService implements AuthInterface {
	@Override
	public boolean signInAuthService(String log, String pass) { //use the google api
		// AuthServiceImpl.signInWithFacebook(log, pass);
		return true; 
	}
}