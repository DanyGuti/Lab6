package OCP;
public class TwitterAuthService implements AuthInterface {
	@Override
	public boolean signInAuthService(String log, String pass) { //use the google api
		// AuthServiceImpl.signInWithTwitter(log, pass);
		return true; 
	}
}