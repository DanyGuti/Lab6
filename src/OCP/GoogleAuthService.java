package OCP;
public class GoogleAuthService implements AuthInterface {
	@Override
	public boolean signInAuthService(String log, String pass) { //use the google api
		// AuthServiceImpl.signInWithGoogle(log, pass);
		return true; 
	}
}